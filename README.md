# HealthBot â€” Phase 0 (Scaffolding)

This creates scaffolding for three services:
- backend-drf
- ai-langchain
- frontend-react

Run locally:
1. Copy `.env.example` -> `.env` for each service and fill secrets.
2. docker-compose up --build
